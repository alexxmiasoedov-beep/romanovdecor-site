{
  "name": "romanov-decor-site",
  "version": "1.0.0",
  "private": true,
  "description": "Сайт Romanov Decor Studio — статика в dist/ + Cloudflare Pages Functions в functions/",
  "scripts": {
    "check": "node scripts/check.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy dist --project-name=romanov-decor --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
