# UNICORE — мозги проекта

Сайт и документы студии микроцемента UNICORE (ИП Мясоедов, Минск).
Бренд в процессе перехода: имя UNICORE уже на сайте, домен пока
`romanovdecor.by` — до покупки `unicore.by` домен и почту не менять.

## Единый дизайн — обязателен для сайта, калькулятора и всех PDF

Эталон и подробности: `документы/СПЕЦИФИКАЦИЯ.md` (раздел про внешний
вид PDF). Краткая выжимка, которой обязано соответствовать всё новое:

**Палитра**

| Роль | Код |
|---|---|
| Графит, основной тёмный | `#2c2c2c` |
| Золото, акцент | `#b8965a` |
| Светлый текст на тёмном | `#efece7` |
| Бежевый фон | `#f5efe2`, линейки `#cbbf9f` |
| Колонка «материал» | шапка `#5a7236`, ячейки `#dde6c5`, текст `#3a4a1c` |
| Колонка «работа» | шапка `#a07a32`, ячейки `#f0d9a8`, текст `#6a4a14` |
| Колонка «итог» | ячейки `#f5e2bb`, слева бордер 3px `#b8965a` |
| Приглушённый текст | `#a09a92` на тёмном, `#888` на светлом |

**Типографика** — Geologica (Google Fonts) с фолбэком Arial, sans-serif.
Заголовки-капсы с letter-spacing 2–3px; мелкие служебные подписи
8–10px капсом с letter-spacing 1.6–2px золотом или приглушённым.

**Логотип** — монограмма U: скруглённый квадрат `#2c2c2c` (радиус
26/120), внутри путь `M 36 30 L 36 58 A 24 24 0 0 0 84 58 L 84 30`,
штрих 15, цвет `#b8965a`, торцы плоские (butt). Название пишется
UNI + CORE, вторая половина золотом. В одну краску — тот же знак
чёрным; мельче 15 мм и на тиснении — версия без разрывов.

**PDF (КП, прайсы)** — A4 = 794×1123 CSS-px, страница обязана
сходиться ровно в 1123 по высоте. Шапка: логотип + имя, справа тип
документа, снизу линия 2px `#b8965a`. Числа: запятая в дробях, пробел
в тысячах, знак € перед числом (€1 120, €58,50). Генераторы —
`документы/kp-generator/*.py`, печать `topdf.js` (playwright-core,
`/opt/pw-browsers/chromium`).

**Эталон вёрстки КП** — `документы/kp-generator/build_kp_losika.py`:
восьмиколоночная таблица «Наименование | Материалы (Объём·Цена·Сумма) |
Работы (Объём·Цена·Сумма) | Итого», словарь стилей `STY` с точными
цветами ячеек, sub-строки итогов, карточки Заказчик/Исполнитель,
строка «ИТОГО К ОПЛАТЕ» с рублёвым эквивалентом, три карточки
Материалы/Работы/Всего, сноска-бокс с золотым бордером слева, футер
«Спасибо за доверие!». Функция `buildPdfHtml` в
`dist/calculator/index.html` несёт копию этих стилей и ОБЯЗАНА
совпадать с эталоном — меняешь вёрстку КП, меняй в обоих местах.

## Тарифная модель — единственный источник правды

Цены в евро, оплата в BYN по курсу НБ РБ на день оплаты. Модель:

- фикс до 20 м²: стены **€1 120**, полы **€1 300**
- базовые ставки 20–35 м², за м²: стены **€35 материал + €21 работа**,
  полы **€43 + €22**
- 35–100 м² — линейная интерполяция («кривая», без ступеней):
  `ставка = hi − (hi − lo) × (S − 35) / 65`
- от 100 м² дно: стены €30 + €18,50, полы €39 + €19,50
- дверное полотно: материал по ставке стен, работа **€45/м²** стороны
- расходники **от €80** на объект; уклон €175; трап €45; трещины
  €4,50/пог.м; примыкания €2,50–9/пог.м; мебель €17,50/м² + €9/пог.м
- поверхности уже 0,5 м считаются в погонных метрах; запас 5%

Числа живут в `документы/kp-generator/costs.py` и
`build_memo_manager.py`; калькулятор сайта
(`dist/calculator/index.html`, константы `MC_RATES`/`mcCurve`)
и страница `dist/price/index.html` обязаны совпадать с ними.
Меняешь цену — меняй во всех трёх местах и пересобирай PDF.

## Технические заметки

- Сайт — статические страницы в `dist/`, без сборки; серверная часть —
  Cloudflare Pages Functions в `functions/`, данные в KV, фото в R2.
- Деплой: push в `main` → GitHub Actions → Cloudflare Pages. Рабочая
  ветка `claude/chat-microcement-migration-v4wwhf` держится равной main.
- Имя бренда встречается и в CSS (`content:"u"` в подвале) — при
  переименованиях искать не только текст в разметке.
- Домен `romanovdecor.by` и почта в canonical/og/sitemap — 284+52
  вхождения, менять только после покупки нового домена.
- Юридическое: заявка на знак UNICORE в НЦИС ещё не подана — бриф
  в `документы/тексты/бриф-для-патентного-поверенного.md`. Для
  интернет-магазина см. `документы/памятки/интернет-магазин-требования-РБ.md`
  (хостинг в РБ обязателен, цены только BYN, безнал без кассы).
